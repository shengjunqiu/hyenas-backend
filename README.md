# Hyenas Monorepo

## Structure

- `backend/`: NestJS backend project
- `frontend/`: frontend project (to be added)
- `docs/`: shared product and task documents

## Quick Start

- Backend: `cd backend && pnpm install && pnpm start:dev`
- Frontend: `cd frontend` and initialize your frontend app
